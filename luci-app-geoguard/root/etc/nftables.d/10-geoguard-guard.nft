set geoguard_ban {
	type ipv4_addr
	flags timeout
}

chain luci_guard {
	type filter hook prerouting priority -160; policy accept;
	iifname "pppoe-wan" tcp dport { 8080, 8081, 2222 } ip saddr @geoguard_ban log prefix "geoguard-ban-drop: " drop
}
